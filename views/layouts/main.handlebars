<!DOCTYPE html >
<html lang="en">
    <head> 
        <meta charset="UTF-8">
        <meta name="viewpoint" content="width=device-width, inital-scale=1.0">
        <title> The Tech Blog </title>
        <link rel="stylesheet" href="">
    </head>

    <body> 
        <div class="wrapper">
            <header>
              <h1>
                  <a href="/"> Tech Blog</a>
              </h1>
              <nav>
                  {{#if loggedIn}}
                  <a href="/dashboard">dashboard</a>
                  <button id="logout" class="btn-no-style">Logout</button>
                  <a href="/login">login</a>
                  {{/if}}
              </nav>
            </header>
            <main>
                {{{ body }}}
            </main>
            <footer>
                Thank you for adding to our blog
            </footer>
        </div>
        {{#if loggedIn}}
        <sript src="/javascript/logout.js"></sript>
        {{/if}}
        </body>
</html>